<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password</title>
    <style>
      body {
        font-family: Arial, Helvetica, sans-serif;
        background-color: #f5f5f5;
        padding: 24px;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 8px;
        padding: 32px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
      }
      h1 {
        color: #111827;
        font-size: 20px;
        margin-bottom: 16px;
      }
      p {
        color: #374151;
        line-height: 1.6;
      }
      .button {
        display: inline-block;
        margin: 24px 0;
        padding: 12px 24px;
        background-color: #2563eb;
        color: #fff;
        text-decoration: none;
        border-radius: 6px;
        font-weight: 600;
      }
      .muted {
        color: #6b7280;
        font-size: 14px;
      }
      .token {
        display: inline-block;
        margin-top: 12px;
        padding: 8px 12px;
        background-color: #f3f4f6;
        border-radius: 4px;
        font-family: "Courier New", Courier, monospace;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Reset Your Password</h1>
      <p>Hi <%= name %>,</p>
      <p>
        We received a request to reset your KarirKit password. Click the button
        below to choose a new password.
      </p>
      <p style="text-align: center">
        <a class="button" href="<%= resetUrl %>" target="_blank" rel="noopener"
          >Reset Password</a
        >
      </p>
      <p>
        If the button doesn't work, copy and paste this link into your browser:
      </p>
      <p>
        <a href="<%= resetUrl %>" target="_blank" rel="noopener"
          ><%= resetUrl %></a
        >
      </p>
      <p>
        Need to enter the code manually? Use this token:
        <span class="token"><%= token %></span>
      </p>
      <p class="muted">
        If you didn't request this, you can safely ignore this email. Your
        password will not change.
      </p>
      <% if (supportEmail) { %>
      <p class="muted">
        Need help? Contact us at
        <a href="mailto:<%= supportEmail %>"><%= supportEmail %></a>.
      </p>
      <% } %>
    </div>
  </body>
</html>
